---
layout: default.hbs
---
<section>
	<div class="section-content">
		<h2>{{title}}</h2>
		{{{content}}}
	</div>
</section>

<section>
	<div class="section-content">
		<ul class="project-list">
			{{#each collections.projects}}
				{{> project-list-item post=this}}
			{{/each}}
		</ul>
	</div>
</section>
